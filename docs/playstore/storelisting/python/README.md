# Python-only store listing frames

**Method:** Pillow composites only (no AI).  
**Out:** `01_hero.png` … `08_control.png` @ 1080×1920  
**Script:** `generate_storelisting.py` (add/run when generating)

Real screenshots from `docs/screenshots/v1/` + logo + brand tokens from `docs/project/ui_design.md`.

Best for: deterministic SSOT, free reruns, exact UI proof.
